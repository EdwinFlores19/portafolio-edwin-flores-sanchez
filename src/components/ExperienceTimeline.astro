---
// ExperienceTimeline.astro - Interactive Enterprise Career Timeline with 17+ Projects
import { experiences } from '../data/experience';

const seidor = experiences[0];
const unjfsc = experiences[1];
---

<section id="experience" class="py-20 px-4 sm:px-6 lg:px-8 relative z-10">
  <div class="max-w-7xl mx-auto space-y-12">
    
    <!-- Section Header -->
    <div class="flex flex-col md:flex-row md:items-end justify-between gap-6">
      <div class="space-y-3 max-w-2xl">
        <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full glass-panel border border-cyan-500/40 text-cyan-300 text-xs font-mono tracking-widest uppercase shadow-neon-cyan/20">
          <span class="w-2 h-2 rounded-full bg-cyan-400 animate-ping"></span>
          <span>Trayectoria & Proyectos Corporativos</span>
        </div>
        <h2 class="text-3xl sm:text-4xl md:text-5xl font-black font-display text-white tracking-tight">
          Experiencia <span class="text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 via-sky-300 to-purple-400">Profesional</span>
        </h2>
        <p class="text-sm sm:text-base text-slate-200 leading-relaxed font-normal">
          Más de 2 años en SEIDOR Consulting participando activamente en más de 17 implementaciones, upgrades y rollouts para líderes en minería, salud, aviación, banca y consumo masivo.
        </p>
      </div>

      <!-- Quick Stats Tag -->
      <div class="glass-panel rounded-2xl p-4 sm:p-5 border border-cyan-500/30 flex items-center gap-5 font-mono">
        <div>
          <span class="block text-2xl sm:text-3xl font-black text-cyan-400">+17</span>
          <span class="block text-xs text-slate-300 uppercase font-semibold">Proyectos Cliente</span>
        </div>
        <div class="w-px h-10 bg-white/15"></div>
        <div>
          <span class="block text-2xl sm:text-3xl font-black text-purple-400">100%</span>
          <span class="block text-xs text-slate-300 uppercase font-semibold">Entregables Éxito</span>
        </div>
      </div>
    </div>

    <!-- Main Company Spotlight: SEIDOR Consulting -->
    <div class="glass-panel rounded-3xl p-6 sm:p-8 lg:p-10 border border-cyan-500/40 shadow-2xl relative overflow-hidden">
      
      <!-- Ambient Glow -->
      <div class="absolute -top-20 -right-20 w-80 h-80 bg-cyan-500/10 rounded-full blur-[120px] pointer-events-none"></div>

      <!-- Company Header -->
      <div class="flex flex-col lg:flex-row lg:items-center justify-between gap-5 pb-6 border-b border-white/10">
        <div class="flex items-center gap-4">
          <div class="w-14 h-14 sm:w-16 sm:h-16 rounded-2xl bg-gradient-to-br from-cyan-500 via-blue-600 to-purple-600 p-[2px] shadow-neon-cyan/30 shrink-0">
            <div class="w-full h-full bg-slate-950 rounded-[14px] flex items-center justify-center font-mono font-black text-cyan-400 text-2xl">
              S
            </div>
          </div>
          <div>
            <div class="flex flex-wrap items-center gap-2.5">
              <h3 class="text-2xl sm:text-3xl font-black text-white font-display">{seidor.company}</h3>
              <span class="px-3 py-1 rounded-full text-xs font-mono font-bold bg-emerald-500/20 text-emerald-300 border border-emerald-500/40">
                Puesto Actual
              </span>
            </div>
            <p class="text-sm sm:text-base text-cyan-300 font-mono mt-1 font-semibold">{seidor.roleTitle}</p>
          </div>
        </div>

        <div class="flex flex-wrap items-center gap-2.5 font-mono text-xs sm:text-sm text-slate-300">
          <span class="px-3.5 py-1.5 rounded-xl bg-white/5 border border-white/10">{seidor.period}</span>
          <span class="px-3.5 py-1.5 rounded-xl bg-white/5 border border-white/10">{seidor.location}</span>
        </div>
      </div>

      <!-- Filter by Industry Pills (Enhanced readability) -->
      <div class="py-6 flex flex-wrap items-center gap-2.5 font-mono text-xs sm:text-sm" id="exp-filters">
        <span class="text-slate-300 mr-2 text-xs uppercase tracking-wider font-bold">Filtrar por Industria:</span>
        <button data-industry="all" class="exp-filter-btn active px-3.5 py-1.5 rounded-xl bg-cyan-400 text-slate-950 font-bold shadow-neon-cyan transition-all">Todas</button>
        <button data-industry="Salud" class="exp-filter-btn px-3.5 py-1.5 rounded-xl glass-panel text-slate-200 hover:text-white font-semibold transition-all">Salud (Clínica Int.)</button>
        <button data-industry="Minería" class="exp-filter-btn px-3.5 py-1.5 rounded-xl glass-panel text-slate-200 hover:text-white font-semibold transition-all">Minería (Antamina, Minsur, Stracon, Zafranal)</button>
        <button data-industry="Financiero" class="exp-filter-btn px-3.5 py-1.5 rounded-xl glass-panel text-slate-200 hover:text-white font-semibold transition-all">Financiero (NUAM)</button>
        <button data-industry="Consumo Masivo" class="exp-filter-btn px-3.5 py-1.5 rounded-xl glass-panel text-slate-200 hover:text-white font-semibold transition-all">Consumo Masivo (ISM, Rokys, Ecuajugos)</button>
        <button data-industry="Aviación" class="exp-filter-btn px-3.5 py-1.5 rounded-xl glass-panel text-slate-200 hover:text-white font-semibold transition-all">Aviación (Talma)</button>
      </div>

      <!-- Projects Grid Inside Seidor -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6" id="experience-cards-grid">
        {seidor.projects.map((proj, idx) => (
          <div
            class="exp-project-card glass-panel rounded-2xl p-6 border border-white/10 hover:border-cyan-400/50 transition-all duration-300 flex flex-col justify-between space-y-4 group"
            data-industry={proj.industry}
          >
            <div class="space-y-3.5">
              <!-- Top Row: Client & Industry Badge -->
              <div class="flex items-center justify-between">
                <span class="px-3 py-1 rounded-md text-xs font-mono font-bold bg-white/5 text-cyan-300 border border-cyan-500/40 uppercase">
                  {proj.industry}
                </span>
                <span class="text-xs font-mono text-slate-300 font-medium">{proj.period}</span>
              </div>

              <!-- Client Title & Role -->
              <div>
                <h4 class="text-xl font-black text-white font-display group-hover:text-cyan-300 transition-colors">
                  {proj.client}
                </h4>
                <p class="text-xs sm:text-sm font-mono font-bold text-slate-200 mt-0.5">{proj.role}</p>
              </div>

              <!-- Impact Highlight Badge -->
              {proj.impactHighlight && (
                <div class="p-3 rounded-xl bg-gradient-to-r from-purple-950/50 to-cyan-950/50 border border-purple-500/40 font-mono text-xs text-purple-200 font-semibold leading-relaxed">
                  ⚡ {proj.impactHighlight}
                </div>
              )}

              <!-- Description list (Enlarged and high contrast) -->
              <ul class="space-y-2 text-xs sm:text-sm text-slate-200 leading-relaxed">
                {proj.description.map(d => (
                  <li class="flex items-start gap-2">
                    <span class="text-cyan-400 font-bold mt-0.5">▪</span>
                    <span>{d}</span>
                  </li>
                ))}
              </ul>
            </div>

            <!-- Bottom Modules & Tech -->
            <div class="pt-4 border-t border-white/10 flex flex-wrap gap-1.5">
              {proj.modules.map(m => (
                <span class="text-xs font-mono px-2.5 py-0.5 rounded bg-cyan-950/60 text-cyan-300 border border-cyan-800/50 font-semibold">
                  {m}
                </span>
              ))}
              {proj.techStack.map(t => (
                <span class="text-xs font-mono px-2.5 py-0.5 rounded bg-white/5 text-slate-200 border border-white/10 font-medium">
                  {t}
                </span>
              ))}
            </div>

          </div>
        ))}
      </div>

    </div>

    <!-- Previous Experience: UNJFSC -->
    <div class="glass-panel rounded-3xl p-6 sm:p-8 border border-white/10">
      <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-3 pb-4 border-b border-white/10">
        <div>
          <h3 class="text-xl font-bold text-white font-display">{unjfsc.company}</h3>
          <p class="text-sm text-slate-300 font-mono mt-0.5">{unjfsc.roleTitle} • {unjfsc.location}</p>
        </div>
        <span class="text-xs sm:text-sm font-mono text-slate-300 px-3.5 py-1.5 rounded-xl bg-white/5 self-start sm:self-auto font-medium">{unjfsc.period}</span>
      </div>
      <p class="text-sm text-slate-200 pt-4 leading-relaxed max-w-4xl font-normal">
        {unjfsc.summary}
      </p>
    </div>

  </div>
</section>

<script>
  // Filter Experience by Industry
  const expBtns = document.querySelectorAll('.exp-filter-btn');
  const expCards = document.querySelectorAll('.exp-project-card');

  expBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      expBtns.forEach(b => {
        b.classList.remove('bg-cyan-400', 'text-slate-950', 'font-bold', 'active');
        b.classList.add('glass-panel', 'text-slate-200');
      });
      btn.classList.remove('glass-panel', 'text-slate-200');
      btn.classList.add('bg-cyan-400', 'text-slate-950', 'font-bold', 'active');

      const ind = btn.getAttribute('data-industry');

      expCards.forEach(card => {
        const cardInd = card.getAttribute('data-industry');
        if (ind === 'all' || cardInd === ind) {
          (card as HTMLElement).style.display = 'flex';
        } else {
          (card as HTMLElement).style.display = 'none';
        }
      });
    });
  });
</script>
