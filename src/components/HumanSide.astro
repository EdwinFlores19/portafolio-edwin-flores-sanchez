---
// HumanSide.astro - Más Allá del Código: Arte, Danza, Voluntariado y Liderazgo
import { humanSideItems } from '../data/humanSide';
import { getAssetUrl } from '../utils/assets';
---

<section id="human" class="py-20 px-4 sm:px-6 lg:px-8 relative z-10 overflow-hidden">
  <div class="max-w-7xl mx-auto space-y-12">
    
    <!-- Section Header -->
    <div class="text-center space-y-4 max-w-3xl mx-auto">
      <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full glass-panel border border-cyan-500/40 text-cyan-300 text-xs font-mono tracking-widest uppercase shadow-neon-cyan/20">
        <span class="w-2 h-2 rounded-full bg-pink-400 animate-pulse"></span>
        <span>Liderazgo, Cultura & Vocación Social</span>
      </div>
      <h2 class="text-3xl sm:text-4xl md:text-5xl font-black font-display text-white tracking-tight">
        Más Allá del <span class="text-transparent bg-clip-text bg-gradient-to-r from-pink-400 via-purple-300 to-cyan-400">Código</span>
      </h2>
      <p class="text-sm sm:text-base text-slate-200 leading-relaxed font-normal">
        La ingeniería de sistemas y la consultoría estratégica se enriquecen cuando se combinan con la sensibilidad del arte escénico, la disciplina colectiva y el compromiso con la comunidad.
      </p>
    </div>

    <!-- Cards Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-6 sm:gap-8">
      {humanSideItems.map((item, idx) => (
        <div class="glass-panel glass-panel-hover rounded-3xl overflow-hidden border border-white/10 hover:border-pink-500/50 transition-all duration-300 flex flex-col justify-between group">
          
          <div class="space-y-4">
            <!-- Image if available -->
            {item.image && (
              <div class="relative h-60 sm:h-64 w-full overflow-hidden bg-slate-950">
                <img
                  src={getAssetUrl(item.image)}
                  alt={item.title}
                  class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700 brightness-95 contrast-105"
                />
                <div class="absolute inset-0 bg-gradient-to-t from-deep-obsidian via-transparent to-transparent"></div>
                <div class="absolute top-4 left-4">
                  <span class="px-3.5 py-1.5 rounded-full text-xs font-mono font-bold bg-slate-950/80 text-pink-300 border border-pink-500/50 backdrop-blur-md uppercase tracking-wider">
                    {item.category}
                  </span>
                </div>
              </div>
            )}

            <!-- Text Content -->
            <div class={`p-6 sm:p-7 ${item.image ? 'pt-0' : 'pt-7'} space-y-3.5`}>
              {!item.image && (
                <span class="px-3.5 py-1.5 rounded-full text-xs font-mono font-bold bg-slate-950/80 text-cyan-300 border border-cyan-500/50 inline-block uppercase tracking-wider">
                  {item.category}
                </span>
              )}
              
              <h3 class="text-2xl font-black text-white font-display group-hover:text-pink-300 transition-colors">
                {item.title}
              </h3>
              <span class="text-xs sm:text-sm font-mono text-cyan-300 block -mt-1 font-semibold">{item.subtitle}</span>

              <p class="text-sm text-slate-200 leading-relaxed font-normal">
                {item.description}
              </p>

              <!-- Highlights list -->
              <ul class="space-y-2 text-xs sm:text-sm text-slate-200 font-mono pt-2">
                {item.highlights.map(h => (
                  <li class="flex items-start gap-2">
                    <span class="text-pink-400 font-bold">▹</span>
                    <span>{h}</span>
                  </li>
                ))}
              </ul>
            </div>
          </div>

          <!-- Bottom Quote -->
          {item.quote && (
            <div class="p-6 sm:p-7 pt-0">
              <blockquote class="p-4 rounded-2xl bg-white/[0.04] border border-white/10 font-mono text-xs sm:text-sm text-slate-200 italic leading-relaxed">
                "{item.quote}"
              </blockquote>
            </div>
          )}

        </div>
      ))}
    </div>

  </div>
</section>
